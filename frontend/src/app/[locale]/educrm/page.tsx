import { Presentation } from "@/ui/Presentation";
import { Metadata } from "next"; export const dynamic = 'force-dynamic'; export async function generateMetadata({ params }: { params: { locale: string } }): Promise<Metadata> { const { locale } = await params; const titles: Record<string, string> = { az: "Neyman Edu CRM Təqdimatı | NeymanTech", en: "Neyman Edu CRM Presentation | NeymanTech", ru: "Презентация Neyman Edu CRM | NeymanTech" }; const descriptions: Record<string, string> = { az: "Kurslar və tədris mərkəzləri üçün rəqəmsal ekosistem: Kanban, rəqəmsal jurnal, maaş və maliyyə modulu.", en: "Digital ecosystem for courses and training centers: Kanban, digital journal, salary and finance module.", ru: "Цифровая экосистема для курсов и учебных центров: Канбан, цифровой журнал, зарплатный и финансовый модули." }; return { title: titles[locale] || titles["az"], description: descriptions[locale] || descriptions["az"] };
} export default async function Page({ params }: { params: { locale: string } }) { const { locale } = await params; return ( <Presentation locale={locale} /> );
} 