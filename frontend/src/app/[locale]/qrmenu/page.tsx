import { QRMenuPresentation } from "@/ui/QRMenuPresentation";
import { Metadata } from "next"; export const dynamic = 'force-dynamic'; export async function generateMetadata({ params }: { params: { locale: string } }): Promise<Metadata> { const { locale } = await params; const titles: Record<string, string> = { az: "Avaz QR Menyu Təqdimatı | NeymanTech", en: "Avaz QR Menu Presentation | NeymanTech", ru: "Презентация Avaz QR Menu | NeymanTech" }; const descriptions: Record<string, string> = { az: "Restoran və kafelər üçün rəqəmsal interaktiv menyu, mətbəx monitor sistemi (KDS) və ağıllı ofisiant zəngi.", en: "Digital interactive menu, kitchen display system (KDS) and smart waiter calling for restaurants and cafes.", ru: "Цифровое интерактивное меню, кухонный монитор (KDS) и умный вызов официанта для ресторанов и кафе." }; return { title: titles[locale] || titles["az"], description: descriptions[locale] || descriptions["az"] };
} export default async function Page({ params }: { params: { locale: string } }) { const { locale } = await params; return ( <QRMenuPresentation locale={locale} /> );
} 